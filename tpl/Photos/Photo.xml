{extends "../@layout.xml"}

{block title}{_"photo"}{/block}

{block header}
    {ifset $album}
        <a href="{$album->getOwner()->getURL()}">
            {$album->getOwner()->getCanonicalName()}
        </a>
        {if ($album->getOwner() instanceof openvk\Web\Models\Entities\Club)}
            » <a href="/albums{$album->getOwner()->getId() * -1}">{_"albums"}</a>
        {else}
            » <a href="/albums{$album->getOwner()->getId()}">{_"albums"}</a>
        {/if}
        » <a href="/album{$album->getPrettyId()}">{$album->getName()}</a>
    {else}
        <a href="{$photo->getOwner()->getURL()}">{$photo->getOwner()->getCanonicalName()}</a>
    {/ifset}
    » {_"photo"}
{/block}

{block titleHeader}
    Фотография
{/block}

{block body}

<div class="photobody">
    <center style="margin-bottom: 8pt;">
        <img src="{$photo->getURL()}" style="max-width: 80%; max-height: 60vh;" />
    </center>
    
    <div class="photolist">
                        <div class="phlistlink">
                            <span class="text-gray">{_"info_description"}: </span><a href="#">{$photo->getDescription() ?? "(отсутствует)"}</a>
                        </div>
                        <div class="phlistlink">
                            <span class="text-gray">{_"info_uploaded_by"}: </span><a href="{$photo->getOwner()->getURL()}">{$photo->getOwner()->getFullName()}</a>
                        </div>
                        <div class="phlistlink">
                            <span class="text-gray">{_"info_upload_date"}: {$photo->getPublicationTime()}</span>
                        </div>
                        <div class="phlistlink" n:if="isset($thisUser) && $thisUser->getId() === $photo->getOwner()->getId()">
                            <a href="/photo{$photo->getPrettyId()}/edit" style="display:block;width:96%;">{_"edit"}</a>
                        </div>
                        <div class="phlistlink" n:if="isset($thisUser) && $thisUser->getId() === $photo->getOwner()->getId()">
                            <a id="_photoDelete" href="/photo{$photo->getPrettyId()}/delete" style="display:block;width:96%;">{_"delete"}</a>
                        </div>
                        <div class="phlistlink" n:if="isset($thisUser) && $thisUser->getId() === $photo->getOwner()->getId()">
                            <a href="{$photo->getURL()}" target="_blank" style="display:block;width:96%;">{_"open_original"}</a>
                        </div>
                    </div>

    <div style="width: 100%; min-height: 100px;">
        <div style=" min-height: 100px; width: 100%;">
            {include "../components/comments.xml", comments => $comments, count => $cCount, page => $cPage, model => "photos", parent => $photo}
        </div>
    </div>
</div>
{/block}